@model LibraryManagement.MVC.Models.Librarian

@{
    ViewData["Title"] = "Delete Librarian";
}

<div class="page-header">
    <div class="container">
        <h1><i class="bi bi-trash text-danger me-2"></i>Delete Librarian</h1>
        <p class="text-danger fw-bold">Are you sure you want to delete this librarian? This action cannot be undone.</p>
    </div>
</div>

<main class="container page-wrapper">
    <div class="card shadow-sm border-danger col-md-8 mx-auto">
        <div class="card-body p-4">
            <h4 class="card-title mb-4">@Model.Name</h4>
            
            <dl class="row mb-0">
                <dt class="col-sm-4 text-muted">@Html.DisplayNameFor(model => model.EmployeeId)</dt>
                <dd class="col-sm-8 fw-bold">@Html.DisplayFor(model => model.EmployeeId)</dd>

                <dt class="col-sm-4 text-muted">@Html.DisplayNameFor(model => model.Shift)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Shift)</dd>
            </dl>
        </div>
        <div class="card-footer bg-white p-4 d-flex justify-content-between">
            <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Cancel</a>
            <form asp-action="Delete" asp-route-id="@Model.Id">
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Delete Librarian</button>
            </form>
        </div>
    </div>
</main>
